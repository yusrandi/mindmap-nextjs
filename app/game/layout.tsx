export const metadata = {
  title: "Materi Data| Next.js 13 Demo App",
};

export default function SubscribeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <section>{children}</section>;
}
