export const metadata = {
  title: "Histories | Next.js 13 Demo App",
};

export default function SubscribeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <section>{children}</section>;
}
